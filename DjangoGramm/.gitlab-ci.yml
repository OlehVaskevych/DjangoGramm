stages:
  - deploy

deploy_to_azure:
  image: curlimages/curl
  stage: deploy
  script:
    - cd DjangoGramm
    - zip -r django_app.zip .
    - curl -T django_app.zip -u "$AZURE_FTP_USERNAME:$AZURE_FTP_PASSWORD" "$AZURE_FTP_SERVER/site/wwwroot/django_app.zip"
  only:
    - main
